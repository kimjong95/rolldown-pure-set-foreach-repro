{
  "name": "rolldown-pure-set-foreach-repro",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "rolldown -c rolldown.config.js",
    "build:toplevel": "rolldown -c rolldown.toplevel.config.js",
    "check": "node scripts/check-dist.mjs",
    "demo": "npm run build && npm run build:toplevel && npm run check"
  },
  "devDependencies": {
    "rolldown": "^1.1.3"
  }
}
