{
  "name": "rolldown-pure-set-foreach-repro",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build:engine": "rolldown engine/src/memento.js --format es --file engine/dist/memento.js",
    "build": "npm run build:engine && npm --prefix vite-app run build && npm --prefix webpack-app run build && npm --prefix webpack-swc-minify-app run build",
    "apps": "node check-apps.mjs"
  },
  "devDependencies": {
    "rolldown": "^1.1.3"
  }
}
